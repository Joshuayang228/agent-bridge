# AGENTS.md — 入口重定向

> **本文件不维护规则。** 本项目的唯一权威规则源是同目录下的 [`CLAUDE.md`](./CLAUDE.md)。
>
> 无论你是 Codex、Cursor 还是其他 Agent，动手前**必须先完整读取 `CLAUDE.md`**，并以其为准。
> 这里不再复制规则正文，是为了避免双源漂移（历史上 AGENTS.md 曾整份复制 CLAUDE.md，导致两边内容不一致）。

## 你需要做的

1. 打开并读完 [`CLAUDE.md`](./CLAUDE.md)——项目定位、安全红线、开发流程闸、场景规则索引都在那里。
2. 按 `CLAUDE.md` 的「场景规则索引」按需读取 `docs/agent-skills/` 下的详细规则。
3. 开始较大任务前，读 `docs/progress.md`（当前状态）和 `docs/decisions.md`（已定决策）。
